# WindowsAPIs
List of ubiquitous Windows APIs that are safe to emit direct calls for.
APIs rarely used and not available on all Windows editions (including Windows 7 and Windows Nano) were removed.

Note: I steal this from `WindowsAPIs.txt` included with `dotnet`.
